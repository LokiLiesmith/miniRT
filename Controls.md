Left click: Select object
when selected: WASD moves the object

Right click hold and drag: Rotate camera
Middle click hold and drag: Pan camera
(on letting go it renders with more samples so more definiton)

Scroll Wheel: Zoom Zoom

N: Toggles multi-threading and single-threaded render (gets really slow)

M: Magic
