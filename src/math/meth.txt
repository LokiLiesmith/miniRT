loop tru pixels:
    ray = create_ray(camera, x, y)
    hit = find_closest_intersection(ray, scene)
    if hit:
        color = compute_lighting(hit, light)
    else:
        color = background
    put_pixel(x, y, color)


	intersection = plug ray equation into the object equation


Sphere:
|P-C|^2 = r^2


P(t) = Origin + t * Direction_v;



	|P − C| exp2 = r exp2